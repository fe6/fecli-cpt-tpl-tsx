import * as React from 'react';
import * as ReactDOM from 'react-dom';
// 引入组件
import { Icon } from '../src/component/index';
// 引入样式
import '../src/styles/index.scss';

ReactDOM.render(<Icon />, document.getElementById('mountNode'));
